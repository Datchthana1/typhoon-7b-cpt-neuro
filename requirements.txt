# ---- core training stack (ตรวจแล้วว่าตรงกับที่ติดตั้งอยู่ในเครื่อง) ----
torch>=2.6.0                 # ติดตั้งอยู่: 2.10.0+cu128
transformers>=4.51.0         # ติดตั้งอยู่: 4.57.6
peft>=0.14.0                 # ติดตั้งอยู่: 0.18.1
accelerate>=1.4.0            # ติดตั้งอยู่: 1.13.0
bitsandbytes>=0.45.0         # ติดตั้งอยู่: 0.49.2  (จำเป็นสำหรับ NF4 + paged optimizer)
datasets>=3.2.0              # ติดตั้งอยู่: 4.8.4
sentencepiece>=0.2.0
safetensors>=0.4.5

# ---- hyperparameter search ----
optuna>=4.2.0
optuna-dashboard>=0.17.0     # optional: ดู study ผ่าน web UI

# ---- data processing ----
datasketch>=1.6.5            # MinHashLSH สำหรับ near-duplicate detection
regex>=2024.11.6
tqdm>=4.67.0
pyyaml>=6.0.2

# ---- benchmark สากล (src/run_benchmark.py) ----
lm-eval                      # ติดตั้งอยู่: เครื่องมือมาตรฐานเบื้องหลัง HF Open LLM Leaderboard
matplotlib                    # สำหรับ src/plot_progress.py และ src/plot_benchmark.py

# ---- optional ----
# pythainlp>=5.0.4           # ตัดคำไทย (ใช้วิเคราะห์คุณภาพข้อความ ไม่บังคับ)
# flash-attn>=2.7.0          # เร็วขึ้น ~15% แต่ build ยากบน Windows — โค้ด fallback เป็น sdpa เอง
# unsloth                    # ลด VRAM ได้อีก ~40% (ดู docs/01_ENVIRONMENT.md)
