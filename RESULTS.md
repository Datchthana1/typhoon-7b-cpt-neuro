# ผลการทดลองจริง — 26 กรกฎาคม 2026

รันบนเครื่อง: **RTX 4060 Laptop 8 GB** · Windows 11 · torch 2.10.0+cu128 · transformers 4.57.6 · peft 0.18.1

> ⚠️ **สถานะของผลชุดนี้: smoke test ที่รันครบทุกขั้นตอนจริง ไม่ใช่ผลของโปรเจกต์ที่เสร็จสมบูรณ์**
> คลังข้อมูลตอนรันมี **8 บทจาก 42 บท** = 15,996 token = **15 บล็อกสำหรับเทรน**
> ตัวเลข PPL ทั้งหมดจึงมี noise สูงมาก (val set มีเพียง **1 บล็อก**)
> สิ่งที่ผลชุดนี้พิสูจน์คือ *ไปป์ไลน์ทำงานถูกต้อง* ไม่ใช่ *โมเดลเรียนรู้เนื้อหาได้ดี*

---

## 1. คลังข้อมูลที่ใช้จริง

| รายการ | ค่า |
|---|---|
| ไฟล์เนื้อหา | 8 บท (`ch01, ch02, ch03, ch13, ch15, ch19, ch20, ch27`) |
| อักขระรวมหลัง clean | 47,847 |
| Token (tokenizer ของ typhoon-7b) | 15,996 → **≈ 3.0 อักขระ/token** |
| dedup exact | 8 → 8 (ไม่มีซ้ำ) |
| dedup near (jaccard > 0.85) | 8 → 8 (ไม่มีซ้ำ) |
| แบ่งระดับเอกสาร | train 7 / val 1 |
| Replay ที่ผสม | 1 เอกสาร Wikipedia ไทย, 12,000 อักขระ (28% ของ train) |
| **บล็อกสุดท้าย** | **train 15 · val 1 · general_val 88** |

Replay corpus ที่เตรียมไว้ทั้งหมด: **400 เอกสาร / 2,653,290 อักขระ** จาก `wikimedia/wikipedia 20231101.th`
General val: **40 เอกสาร / 288,372 อักขระ** → pack ได้ 88 บล็อก

---

## 2. Baseline — typhoon-7b ก่อนเทรน

| ตัวชี้วัด | ค่า | จำนวนบล็อก |
|---|---|---|
| PPL โดเมนใหม่ (บทความ neuroplasticity) | **11.073** | 1 |
| PPL ภาษาไทยทั่วไป (Wikipedia held-out) | **8.034** | 88 |

PPL 8.03 บน Wikipedia ไทยเป็นค่าที่สมเหตุสมผลสำหรับโมเดล 7B ที่ pre-train ภาษาไทยมาแล้ว
PPL ที่สูงกว่าบนโดเมนใหม่ (11.07) แสดงว่าเนื้อหาแนวสารคดีประสาทวิทยาศาสตร์
อยู่นอกการกระจายที่โมเดลคุ้นเคย — ซึ่งเป็นเงื่อนไขที่ถูกต้องสำหรับการทำ CPT

---

## 3. การทดลองที่ 1 — hyperparameter เชิงรุก (**ล้มเหลว**)

```
learning_rate = 1e-4 · lora_r = 32 · alpha = 64 · grad_accum = 4 · epochs = 8
```

| ตัวชี้วัด | ก่อน | หลัง | เปลี่ยนแปลง |
|---|---|---|---|
| PPL โดเมนใหม่ | 11.073 | **45.127** | 🔴 **+307.6%** |
| PPL ไทยทั่วไป | 8.034 | **38.857** | 🔴 **+383.7%** |
| train loss สุดท้าย | — | **0.0050** | |
| eval loss สุดท้าย | — | 3.804 | |
| VRAM peak | — | 6.96 GB / 8.0 GB | |
| เวลา | — | 9 นาที 35 วินาที (32 step) | |

### วิเคราะห์

`train_loss = 0.005` คู่กับ `eval_loss = 3.80` คือลายเซ็นของ **การท่องจำ** ไม่ใช่การเรียนรู้
โมเดลจำ 15 บล็อกได้หมดภายใน 32 step แล้วสูญเสียความสามารถทั่วไปไปพร้อมกัน

**บทเรียนที่สำคัญที่สุดจากการรันนี้:** ถ้า objective ของ Optuna ใช้ `domain_loss` อย่างเดียว
มันจะเลือกค่าชุดนี้ว่า "ดี" เพราะ train loss ต่ำมาก ทั้งที่มันทำลายโมเดล
นี่คือหลักฐานเชิงประจักษ์ว่า **forgetting penalty ใน `configs/optuna.yaml` ไม่ใช่ของประดับ**

---

## 4. การทดลองที่ 2 — hyperparameter อนุรักษ์ (**สำเร็จ**)

```
learning_rate = 1.5e-5 · lora_r = 16 · alpha = 16 · grad_accum = 4 · epochs = 2
```

| ตัวชี้วัด | ก่อน | หลัง | เปลี่ยนแปลง |
|---|---|---|---|
| PPL โดเมนใหม่ | 11.073 | **10.051** | 🟢 **−9.2%** |
| PPL ไทยทั่วไป | 8.034 | **7.725** | 🟢 **−3.8%** (ดีขึ้น ไม่ลืม) |
| VRAM peak | — | 6.44 GB / 8.0 GB | |
| พารามิเตอร์ที่เทรน | — | 41,943,040 / 3,820,384,256 (**1.098%**) | |
| เวลา | — | 2 นาที 12 วินาที (8 step) | |

### เส้นทางของ eval loss

| step | epoch | eval_loss |
|---|---|---|
| 2 | 0.53 | 2.3778 |
| 4 | 1.00 | 2.3283 |
| 6 | 1.53 | 2.2970 |
| 8 | 2.00 | **2.2912** |

ลดลงต่อเนื่องแบบ monotonic ไม่มีสัญญาณ overfit — เส้นโค้งสุขภาพดี

---

## 5. เปรียบเทียบสองการทดลอง

| | เชิงรุก | อนุรักษ์ | ต่างกัน |
|---|---|---|---|
| learning_rate | 1e-4 | 1.5e-5 | **6.7×** |
| lora_r | 32 | 16 | 2× |
| epochs | 8 | 2 | 4× |
| optimizer step | 32 | 8 | 4× |
| **PPL โดเมนใหม่** | 45.13 | **10.05** | **4.5×** |
| **PPL ไทยทั่วไป** | 38.86 | **7.73** | **5.0×** |

ตัวแปรที่ต่างกันมากที่สุดคือ `learning_rate` ซึ่งสอดคล้องกับที่คาดไว้ใน `docs/06_OPTUNA_HPO.md`
ว่า `learning_rate` มักครองสัดส่วนความสำคัญเกิน 50% ใน fANOVA

---

## 6. บั๊กที่พบและแก้ระหว่างรันจริง

| # | อาการ | สาเหตุ | การแก้ |
|---|---|---|---|
| 1 | `UnicodeEncodeError: 'charmap' codec can't encode '→'` | Windows console เริ่มต้นเป็น **cp874** ซึ่ง encode ภาษาไทย + ลูกศรไม่ได้ | เพิ่ม `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` ใน `src/utils.py:get_logger()` |
| 2 | เทรนไม่ครบแม้แต่ 1 optimizer step | 15 บล็อก ÷ `grad_accum=16` < 1 | ทำให้ `eval_steps`/`logging_steps`/`save_steps` override ได้จาก `--override` ใน `src/train_cpt.py:build_args()` |
| 3 | รันจบด้วย exit 0 แต่ไม่มี adapter ถูกบันทึก | `load_best_model_at_end=True` บังคับให้ `save_steps` ต้องหารลงตัวด้วย `eval_steps` — ตั้ง `save_steps=1000, eval_steps=3` จึง raise ก่อนถึงบรรทัดบันทึก | ใช้ค่าที่หารลงตัว (`eval_steps=2, save_steps=1000`) |

ทั้งสามข้อถูกแก้ในโค้ดแล้ว

---

## 6.5 Generation probe — หลักฐานที่สำคัญที่สุดในรายงานนี้

รัน `python src/evaluate.py --probe --adapter outputs/conservative/adapter`
ผลลัพธ์แยกเป็นสองด้านที่ตรงกันข้ามกันอย่างชัดเจน

### ✅ ด้านที่สำเร็จ — รูปแบบข้อความถูกต้องตามเป้าหมาย

โมเดล **ต่อประโยคเป็นร้อยแก้วต่อเนื่อง** ไม่มี bullet ไม่มี Q&A ไม่มี markdown
ซึ่งยืนยันว่าไปป์ไลน์ทำ CPT ไม่ใช่ SFT โดยไม่ตั้งใจ

### 🔴 ด้านที่ล้มเหลว — เนื้อหาเป็นการแต่งขึ้นทั้งหมด

| prompt | สิ่งที่ควรได้ (ตาม FACT_TABLE) | สิ่งที่โมเดลตอบจริง |
|---|---|---|
| "งานวิจัยเรื่องคนขับแท็กซี่ในกรุงลอนดอนแสดงให้เห็นว่า" | ฮิปโปแคมปัสส่วนหลังใหญ่ขึ้นตามจำนวนปีที่ขับ (Maguire 2000) | สถิติประชากรศาสตร์ของคนขับแท็กซี่ — "ร้อยละ 86 ของผู้โดยสารมาจากเอเชีย" **ไม่เกี่ยวกับสมองเลย** |
| "ไมอีลินคือปลอกไขมันที่ห่อหุ้มแอกซอน ซึ่งทำหน้าที่" | เพิ่มความเร็วการนำสัญญาณแบบ saltatory conduction | สับสนไมอีลินกับ myelin sheath แล้วไหลไปเรื่องวิตามินบี 12 และอ้างว่าไมอีลินเสียหาย → **โรคพาร์กินสัน** (ผิด) |
| "ความเชื่อที่ว่าการสร้างนิสัยใหม่ต้องใช้เวลายี่สิบเอ็ดวันนั้น" | มาจาก Maltz 1960 · Lally 2010 มัธยฐาน 66 วัน | **แต่งทฤษฎีขึ้นมาใหม่ทั้งหมด** — "Kohls Cue Theory", "Stone's Habit Formation Theory", "Maslow's Habit Formation Theory" อ้างงานวิจัยปี 1937 ที่ไม่มีอยู่จริง และสับสน Maxwell Maltz กับ Maslow |

### สรุปข้อนี้

**PPL ที่ลดลง 9.2% คือ noise ไม่ใช่การเรียนรู้** — probe พิสูจน์ข้อนี้ชัดเจนกว่าตัวเลขใด ๆ
โมเดลยังคงเป็น `typhoon-7b` ตั้งต้นแทบทุกประการ เพราะ 8 optimizer step บน 15 บล็อก
ไม่เพียงพอที่จะเปลี่ยนน้ำหนักในระดับที่ฝังความรู้ได้

นี่คือเหตุผลที่ **generation probe ต้องเป็นเกณฑ์บังคับก่อนเผยแพร่โมเดล**
ถ้าดูแต่ PPL อย่างเดียวจะสรุปผิดว่า "โมเดลดีขึ้น 9.2%" แล้ว push ขึ้น Hugging Face
ทั้งที่โมเดลยังแต่งชื่อทฤษฎีและปีงานวิจัยขึ้นมาเอง

---

## 7. สิ่งที่ผลชุดนี้ **ไม่ได้** พิสูจน์

1. **ไม่ได้พิสูจน์ว่าโมเดลเรียนรู้เนื้อหาเรื่อง neuroplasticity ได้**
   PPL ลด 9.2% บน val ที่มี **1 บล็อก** อยู่ในระดับที่แยกจาก noise ไม่ได้
   ต้องมี val อย่างน้อย 30–50 บล็อกจึงจะสรุปได้

2. **ไม่ได้พิสูจน์ว่าค่า hyperparameter ชุดอนุรักษ์คือค่าที่ดีที่สุด**
   เป็นเพียงจุดที่ปลอดภัย ยังไม่ได้รัน Optuna เต็ม (ต้องมีข้อมูลมากกว่านี้ก่อน)

3. **ไม่ได้รัน Optuna** — ด้วย train 15 บล็อก แต่ละ trial จะจบใน 8 step
   ซึ่ง MedianPruner ทำงานไม่ได้ และ objective มี noise ท่วม signal

---

## 8. ขั้นตอนต่อไปตามลำดับความสำคัญ

| ลำดับ | สิ่งที่ต้องทำ | เกณฑ์ว่าพอแล้ว |
|---|---|---|
| 1 | เขียนบทที่เหลือ 34 บท | คลัง ≥ 350K token → ≥ 340 บล็อก |
| 2 | ปรับ `val_ratio` เป็น 0.10 | val ≥ 35 บล็อก → PPL มีความหมายทางสถิติ |
| 3 | รัน `python main.py hpo` เต็ม 25 trials | คาด 4–6 ชม. บนเครื่องนี้ |
| 4 | `python main.py train --from-study` | domain PPL ควรลด > 15% |
| 5 | `python main.py merge` แล้วจึงพิจารณา push | ผ่านเกณฑ์ข้อ 4 ก่อน |

---

## 9. การอัปโหลดขึ้น Hugging Face — เสร็จแล้ว

**Repo:** [`Datchthana/typhoon-7b-neuro-cpt`](https://huggingface.co/Datchthana/typhoon-7b-neuro-cpt)
**สถานะ: 🌐 public** (เปลี่ยนจาก private ตามที่เจ้าของโปรเจกต์ตัดสินใจ)

| ไฟล์ | ขนาด |
|---|---|
| `adapter_model.safetensors` | 160.1 MB |
| `adapter_config.json` | < 1 MB |
| `tokenizer.json` · `tokenizer.model` · configs | 4.2 MB |
| `README.md` (model card) | < 1 MB |

คำสั่งที่ใช้:
```powershell
python src/push_to_hub.py --repo Datchthana/typhoon-7b-neuro-cpt --adapter outputs/conservative/adapter
```

### หมายเหตุเรื่องสถานะสาธารณะ

adapter นี้ผ่าน quality gate ที่ดู PPL (domain −9.2%, general −3.8%)
แต่ **ไม่ผ่านการตรวจด้วย generation probe** (ดู §6.5) — โมเดลยังแต่งชื่อทฤษฎีและปีงานวิจัยขึ้นเอง

เดิมอัปโหลดเป็น private แล้วเปลี่ยนเป็น public ตามการตัดสินใจของเจ้าของโปรเจกต์
โดยอาศัย model card เป็นตัวสื่อสารข้อจำกัดแทน

เกณฑ์ 5 ข้อสำหรับการเผยแพร่โมเดลที่พร้อมใช้งานจริงยังอยู่ที่
[docs/08_DEPLOY_MERGE.md](docs/08_DEPLOY_MERGE.md#เกณฑ์ก่อนเผยแพร่สู่สาธารณะ)
และควรกลับมาผ่านให้ครบก่อนถอดคำเตือนออกจาก model card

### model card ระบุข้อจำกัดไว้ครบ

การ์ดที่อยู่บน hub มีหัวข้อ **"⚠️ ข้อจำกัดที่ต้องอ่านก่อนใช้"** อยู่ก่อนตัวอย่างโค้ด
ระบุชัดเจนว่าเป็น smoke test, เทรนบน 15 บล็อก (15,360 token),
โมเดลยังหลอนข้อเท็จจริง, PPL ที่ดีขึ้นเป็น noise, และห้ามใช้อ้างอิงทางการแพทย์หรือวิชาการ

**เมื่อคลังข้อมูลครบ 42 บทและเทรนใหม่แล้ว ต้องอัปเดต model card ด้วย**
ไม่งั้นคำเตือนที่ล้าสมัยจะกลายเป็นข้อมูลผิดอีกชุดหนึ่ง:

```powershell
python src/push_to_hub.py --repo Datchthana/typhoon-7b-neuro-cpt --public
```

---

## 10. บันทึกด้านความปลอดภัย

Access token ถูกเก็บไว้ 2 ที่:

| ที่เก็บ | หมายเหตุ |
|---|---|
| `.env` | อยู่ใน `.gitignore` แล้ว (บรรทัด 2–3) จะไม่ถูก commit |
| HF credential store | `~/.cache/huggingface/token` — SDK และ CLI อ่านเองอัตโนมัติ |

> ⚠️ **token ตัวที่ใช้ในเซสชันนี้ถูกส่งผ่าน chat จึงถือว่าหลุดแล้ว**
> ควรเพิกถอนที่ https://huggingface.co/settings/tokens แล้วสร้างใหม่

---

## 11. รันเต็มสเกล (outputs_large_v2) — 29 ก.ค.–1 ส.ค. 2026 — **ผลลบ**

รันจริงครบ 3 epoch / 5,853 step บนคลัง 15,607 บล็อก (~16M token unique) ใช้เวลา ~65 ชม.
(รวมเวลาที่เสียไปกับเครื่องแครช 3 ครั้งระหว่างทาง — ดูหัวข้อ "บั๊กที่พบระหว่างรันจริง" ด้านล่าง)

### ผลการประเมิน (เทียบกับ typhoon-7b ดั้งเดิม ไม่ผ่าน CPT)

| ตัวชี้วัด | Base (ไม่ CPT) | Tuned (รอบนี้) | เปลี่ยนแปลง |
|---|---|---|---|
| **PPL โดเมนใหม่** (266 บล็อก) | 9.858 | 10.507 | 🔴 **+6.6% (แย่ลง)** |
| PPL ภาษาไทยทั่วไป (88 บล็อก) | 8.034 | ~~3.741~~ | ⚠️ **ใช้ไม่ได้ — มี data leakage (ดูล่าง)** |

**สรุป: ไม่ผ่านเกณฑ์คุณภาพ** (`src/push_to_hub.py`'s quality gate ปฏิเสธการ push อัตโนมัติ)
โมเดลดั้งเดิมที่ไม่ผ่าน CPT เลยยังทายข้อความโดเมนนี้ได้แม่นกว่าตัวที่เทรนจบแล้ว — CPT รอบนี้ทำให้แย่ลง ไม่ใช่ดีขึ้น

### สาเหตุที่ 1: PPL ภาษาไทยทั่วไปดูดีผิดปกติ (-53%) → data leakage จริง

`data/replay/general_val.jsonl` (ชุด held-out สำหรับวัด catastrophic forgetting) อยู่ในโฟลเดอร์เดียวกับที่
`src/prepare_data.py` กวาดไฟล์ `*.jsonl` มาผสมเป็น replay corpus สำหรับ**เทรน** โดยไม่มีการยกเว้นไว้
โมเดลจึงน่าจะเคยเห็นข้อความชุดนี้ระหว่างเทรนมาแล้ว ตัวเลข PPL ต่ำผิดปกติเพราะ**ท่องจำ** ไม่ใช่เข้าใจภาษาไทยทั่วไปดีขึ้นจริง

**แก้แล้ว:** `src/prepare_data.py` ตอนกวาดไฟล์ replay ตอนนี้ข้ามไฟล์ที่ชื่อขึ้นต้นด้วย `general_val` แล้ว

### สาเหตุที่ 2: PPL โดเมนใหม่แย่ลงจริง (ไม่มี leakage) → learning rate สูงเกินไป

ตัวเลขนี้วัดถูกต้อง (train/val แยกระดับเอกสารก่อน pack ตั้งแต่แรกอยู่แล้ว) หลักฐานที่ชี้ไปทาง `learning_rate=8e-5` สูงไป:

- ช่วง step 0–150 (warmup) lr พุ่งขึ้นเร็ว และ train loss พุ่งขึ้นตาม (2.15 → 2.42 → 2.53) — สัญญาณ "warmup shock"
- eval_loss ที่จุดแรกที่วัดได้ (step 500) = 2.6027 — **แย่กว่า baseline loss (2.288) ตั้งแต่จุดแรกที่มีข้อมูล**
- ตลอด step 500→5500 eval_loss ฟื้นตัวต่อเนื่องจาก 2.60 → 2.35 แต่**ไม่เคยกลับไปต่ำกว่า baseline เลยสักจุด**
  แม้แต่ checkpoint ที่ดีที่สุดที่ `load_best_model_at_end` เลือกให้ (step 5500) ก็ยังแพ้ baseline

ตรงกับรูปแบบใน §3 (การทดลองเชิงรุก lr=1e-4 ล้มเหลว) เป๊ะ แค่ไม่รุนแรงเท่า เพราะคลังใหญ่กว่าและมีหลาย epoch ช่วยพยุงไว้บางส่วน
**บทเรียน: lr=8e-5 ยังถือว่า "เชิงรุก" สำหรับคลังขนาดนี้ ไม่ใช่ "อนุรักษ์" อย่างที่คอมเมนต์ใน config สันนิษฐานไว้แต่แรก**

### บั๊กที่พบระหว่างรันจริง (นอกเหนือจาก data leakage ด้านบน)

| # | อาการ | สาเหตุ | การแก้ |
|---|---|---|---|
| 1 | เครื่อง BSOD (`nvlddmkm.sys`, bugcheck `0x1e`) กลางดึก 3 ครั้ง | ตรงกับจังหวะเซฟ `optimizer.pt` ของ `paged_adamw_8bit` ทุกครั้ง (ไม่เคยแครชกลาง step ปกติเลย) | เพิ่ม `save_only_model=True` ใน `TrainingArguments` — ข้ามการเซฟ optimizer/scheduler/rng ไปเลย เพราะโหลดกลับมาใช้ไม่สำเร็จสักครั้งอยู่แล้ว |
| 2 | resume จาก checkpoint ที่แครชกลางเซฟ → `FileNotFoundError` ทันที | `get_last_checkpoint()` หยิบ checkpoint ล่าสุดแบบไม่ตรวจสอบว่าไฟล์ครบไหม | เพิ่ม `find_resumable_checkpoint()` ใน `src/train_cpt.py` เช็ค `trainer_state.json` ก่อน ถ้าไม่ครบเปลี่ยนชื่อเป็น `.broken` แล้วถอยไปใช้อันก่อนหน้า |
| 3 | ทุกครั้งที่ resume, cosine LR schedule รีเซ็ตกลับไปเริ่มใหม่จาก peak | ไม่มี `scheduler.pt` ให้โหลด (เพราะบั๊ก #1) และ `create_scheduler()` ไม่มีกลไก fast-forward ตาม `global_step` | เพิ่มโค้ด fast-forward `trainer.lr_scheduler` ด้วยมือตาม `global_step` ที่โหลดมาจาก checkpoint ก่อนเริ่มเทรนต่อ |
| 4 | eval_loss บาง step หายไปจาก log แม้ eval รันจบจริง | stdout buffering ตอน redirect ไปไฟล์ ค้างจนกว่า buffer เต็มถึงจะ flush | ใช้ `PYTHONUNBUFFERED=1` ตอนรัน + เพิ่ม `MetricHistoryCallback` เขียน `eval_metrics.jsonl` ทันทีด้วย `flush()+os.fsync()` แยกจาก stdout โดยสิ้นเชิง |

### สถานะบน Hugging Face

Push ไปที่ repo เดิม [`Datchthana/typhoon-7b-neuro-cpt`](https://huggingface.co/Datchthana/typhoon-7b-neuro-cpt)
(ทับ adapter รอบ smoke test เดิมใน §9) พร้อม model card ที่เขียนอธิบายผลลบนี้ไว้ตรงไปตรงมา
**ข้าม quality gate โดยตั้งใจ** (`--skip-quality-gate` เทียบเท่า) เพื่อเก็บบันทึกไว้เป็นหลักฐานงานวิจัย ไม่ใช่เพราะคิดว่าผ่านเกณฑ์

### แผนแก้ไขรอบถัดไป

1. ลด `learning_rate` จาก 8e-5 เหลือแถว 1.5e-5–3e-5
2. รันด้วย `src/prepare_data.py` เวอร์ชันที่แก้ leakage แล้ว
3. ใช้เครื่องมือใหม่ที่เพิ่มเข้ามารอบนี้ช่วยติดตาม: `src/plot_progress.py` (กราฟ eval_loss), `src/run_benchmark.py` +
   `src/plot_benchmark.py` (เทียบ benchmark สากล: `belebele_tha_Thai`, `xcopa_th`, `mmlu_th_llama`)
4. push ทับ `Datchthana/typhoon-7b-neuro-cpt` อีกครั้งพร้อมอัปเดตตารางผลในหัวข้อนี้และใน model card เมื่อรันเสร็จ
> จากนั้นแก้เฉพาะบรรทัด `HF_TOKEN=` ใน `.env` และรัน `hf auth login --token <ใหม่>`

---

## 12. เตรียม v3 — แก้ data source + config ตามแผนใน §11 (1 ส.ค. 2026) — **ยังไม่ได้รัน**

ทำตามแผนแก้ไขใน §11 ครบทั้ง 2 ข้อแรก พบปัญหาข้อมูลที่ใหญ่กว่าที่คาดระหว่างทาง เลยขยายขอบเขตการแก้:

### พบว่า `data/raw/wiki_domain.jsonl` (130MB, 8,127 เอกสาร) เป็นขยะ >99%

สุ่มตรวจ 15 เอกสารจากไฟล์นี้ (ที่ใช้เป็น "คลังโดเมน" ในการเทรนรอบ §11 ทั้งหมด) พบว่าเป็นบทความ Wikipedia
ทั่วไปที่ไม่เกี่ยวกับประสาทวิทยาเลย: **เต่า, เบียร์ดดราก้อน, David Bowie, หนัง Exit Wounds, วิทยาลัยสาธารณสุข,
แมงกะพรุน, การ์ตูน Galaxy Express 999, พระนักเผยแผ่, สรีรวิทยาระบบนิเวศ, Crash Bandicoot, พลเอกไทย,
สวนสาธารณะ, วิดีโอเกม Remember Me, ไทรโลไบต์, เลข 0** — มีแค่ 1/15 ที่เกี่ยวข้องจริง (แมงกะพรุน มีคำ
"เซลล์ประสาท"+"ระบบประสาท" จริง เพราะบทความพูดถึงวิวัฒนาการระบบประสาทดั้งเดิม)

**สาเหตุ:** `src/build_corpus.py` เดิมให้คะแนนจากคำสะสม (CORE×3 + MID×2 + WEAK×1) แล้วผ่านเกณฑ์ที่ `min_score=6`
โดยไม่ต้องมีคำเฉพาะทางประสาทวิทยา (CORE) เลยสักคำ — บทความสัตว์/ชีววิทยาทั่วไปผ่านง่ายมากเพราะมักมีคำอย่าง
"วิวัฒนาการ"+"เซลล์"+"เลือด"+"อวัยวะ" (MID/WEAK ทั้งหมด) โดยไม่แตะประสาทวิทยาเลย

คลังที่แท้จริงใช้ในการเทรน §11 จึงเป็น book chapter จริง (~200KB) จมอยู่ใต้ Wikipedia ทั่วไป 130MB
— **นี่คือสาเหตุร่วมอีกข้อที่ domain PPL ไม่ดีขึ้น** (เพิ่มเติมจาก lr สูงไปที่วิเคราะห์ไว้ใน §11)
โมเดลใช้ compute ส่วนใหญ่ไปเรียนเรื่องเต่ากับ David Bowie แทนที่จะเป็น neuroplasticity

### แก้ไข

**`src/build_corpus.py`:** `score()` คืนค่า `(total, n_core)` แยกกัน บังคับให้ต้องมี `core_hits >= 1`
เสมอ (ตั้ง `--min-core-hits`) ไม่ใช่แค่คะแนนสะสมผ่านเกณฑ์

**รันใหม่** (`--min-score 10 --min-core-hits 1`): ทดสอบ 3 ระดับความเข้มงวดก่อนตัดสินใจ

| การตั้งค่า | เอกสาร | token | สุ่มตรวจคุณภาพ |
|---|---|---|---|
| เดิม (ไม่มี core requirement) | 8,127 | ~43M chars | ~1/15 เกี่ยวข้อง |
| min_score=4, core≥1 | 2,067 | 4.4M | หลุดกลับมาอีก (คำ CORE ที่กว้าง เช่น "การเรียนรู้" ไปตรงกับ Kamen Rider, Final Fantasy VI โดยบังเอิญ) |
| min_score=8, core≥2 | 488 | 1.0M | แม่นแต่ได้ volume น้อยไป |
| **min_score=10, core≥1 (เลือกใช้)** | **606** | **1.77M** | สุ่มตรวจ 30 เอกสาร ~55% เกี่ยวข้องชัดเจน (ทฤษฎีการเรียนรู้, Theory of Mind, migraine, agnosia, cerebellar ataxia, method of loci, ...) ที่เหลือเป็นวิทยาศาสตร์/การแพทย์ทั่วไปที่ไม่ตรงประเด็นแต่ไม่ใช่ขยะสุ่มแบบเดิม |

**ข้อจำกัดที่ยอมรับ:** การกรองด้วย keyword ไม่มีทางแม่น 100% — คำ CORE บางคำ (การเรียนรู้, ความทรงจำ, จิตวิทยา)
กว้างพอที่จะเจอในบริบทไม่เกี่ยวข้องได้ (ตัวละครการ์ตูน "จำ" อะไรไม่ได้, คดีอาชญากรรมพูดถึง "พฤติกรรม")
ถ้าต้องการแม่นกว่านี้จริง ๆ ต้องใช้ LLM ช่วยตัดสิน relevance ทีละเอกสารแทน keyword matching — ยังไม่ได้ทำ
เพราะเพิ่มความซับซ้อน/ต้นทุนอีกชั้น ทางเลือกที่คุ้มค่าที่สุดถ้าต้องการคลังใหญ่และแม่นขึ้นจริงคือ**เขียนบทหนังสือ
เพิ่ม** (ยังมีแค่ 8/42 บทที่เขียนจบเต็มที่) เพราะเนื้อหาที่เขียนเองตรงประเด็น 100% อยู่แล้ว ไม่ต้องกรอง

**คลังสุดท้ายหลัง `prepare_data.py`:** 1,746 train บล็อก (1.79M token) + 40 val + 88 general_val
เทียบกับ 15,607 บล็อกของ §11 ที่ token ส่วนใหญ่ไม่เกี่ยวข้องเลย

### Config ใหม่ — `configs/cpt_large_v3.yaml`

| พารามิเตอร์ | v2 (§11, ผลลบ) | v3 | เหตุผล |
|---|---|---|---|
| `learning_rate` | 8e-5 | **2e-5** | v2 พิสูจน์ว่า 8e-5 ทำให้ domain PPL แย่กว่า baseline ตลอดการเทรน — 2e-5 อยู่กึ่งกลางระหว่าง 1.5e-5 ที่เคยสำเร็จ (§4) กับขอบล่างเดิมของ Optuna search space |
| `lora_r` / `alpha` | 64 / 128 | **32 / 64** | คลังจริงเล็กกว่าที่ v2 ออกแบบไว้มาก (1.79M ไม่ใช่ 30-40M token ที่คอมเมนต์เดิมสมมติ) — r สูงบนคลังเล็กเสี่ยง overfit |
| `epochs` | 3 | **15** | ไม่จำกัดเวลาตามที่ระบุ + คลังเล็กลงมาก (219 step/epoch แทน 1,950) รวม 15 epoch ≈ 3,270 step ยังน้อยกว่า 5,853 step ของ v2 ครึ่งหนึ่ง |
| `warmup_ratio` | 0.02 | **0.03** | ลดโอกาส "warmup shock" ที่เห็นใน v2 (train loss พุ่งช่วง step 0-150) |
| `eval_steps`/`save_steps` | 500 | **50** | คลังเล็กลงมาก 500 step เดิมจะเห็น eval แค่ ~6 จุดตลอดการเทรน ไม่พอจับ overfit ทัน |

**Optuna search space** (`configs/optuna.yaml`) ก็ปรับ `learning_rate` จาก `3e-5–4e-4` เป็น `5e-6–2e-4`
เพราะช่วงเดิมทั้งช่วงเอนไปทาง "เชิงรุก" เกินไป (8e-5 ที่อยู่ในช่วงเดิมยังพังอยู่)

**บั๊ก crash-resilience ทั้งหมดจาก §11** (save_only_model, find_resumable_checkpoint, LR fast-forward,
MetricHistoryCallback, PYTHONUNBUFFERED) ใช้ร่วมกับ v3 ได้ทันทีโดยไม่ต้องแก้อะไรเพิ่ม

### สถานะ: รันแล้ว → ดูผลที่ §13

ไฟล์เก่าที่ถูกแทนที่เก็บสำรองไว้ที่ `data/domain_old_unfiltered/` (ไม่ commit ขึ้น git — ใหญ่เกิน 100MB)
คำสั่งรัน: `run_train_v3.bat` หรือ `python src/train_cpt.py --config configs/cpt_large_v3.yaml --out outputs_large_v3/run1`

---

## 13. รัน v3 (1–2 ส.ค. 2026) — 🟢 **สำเร็จ ผ่านเกณฑ์คุณภาพ**

การรันแรกในโปรเจกต์นี้ที่ได้ผลดีขึ้นจริงบนชุดทดสอบที่เชื่อถือได้ (val 40 บล็อก ไม่ใช่ 1 บล็อกแบบ §3–4
และไม่มี data leakage แบบ §11)

### ผลการประเมิน (เทียบ typhoon-7b ดั้งเดิม บนชุดทดสอบเดียวกัน)

| ตัวชี้วัด | Base (ไม่ CPT) | v3 (checkpoint-200) | เปลี่ยนแปลง |
|---|---|---|---|
| **PPL โดเมนประสาทวิทยา** (40 บล็อก) | 8.830 | **7.442** | 🟢 **−15.7%** |
| **PPL ภาษาไทยทั่วไป** (88 บล็อก) | 8.034 | **7.024** | 🟢 **−12.6%** |

✅ ผ่าน quality gate ใน `src/push_to_hub.py` (domain ต้องลด ≥5%, general ต้องไม่เพิ่มเกิน 5%)
เผยแพร่ที่ [`Datchthana/typhoon-7b-neuro-cpt`](https://huggingface.co/Datchthana/typhoon-7b-neuro-cpt)

### เส้นทางของ eval_loss — เจอ overfitting ชัดเจนหลัง step 400

| step | epoch | eval_loss | PPL | หมายเหตุ |
|---|---|---|---|---|
| 50 | 0.23 | 2.0530 | 7.792 | ดีกว่า baseline ตั้งแต่จุดแรก (ต่างจาก §11 ที่ไม่เคยแซงเลย) |
| 200 | 0.92 | **2.0050** | **7.426** | 🟢 **ดีที่สุด — checkpoint ที่เผยแพร่** |
| 250 | 1.14 | 2.0265 | 7.588 | ขึ้นเล็กน้อย (รอยต่อ epoch) |
| 400 | 1.83 | 2.0211 | 7.546 | ยังทรงตัว |
| 700 | 3.20 | 2.4640 | 11.75 | 🔴 เริ่มพุ่ง |
| 900 | 4.11 | 3.0163 | 20.42 | 🔴 แย่ลงหนักขึ้นทุก epoch |
| 1100 | 5.02 | 3.3631 | 28.88 | 🔴 แย่กว่า baseline เกือบ 3 เท่า |

train loss ที่ step 1120 ลงถึง **0.3116** — ลายเซ็นของ **การท่องจำ (memorization)** ตรงกับรูปแบบใน §3
จึงหยุดการเทรนที่ step ~1130 (จาก 3,285 ที่ตั้งไว้) แล้วใช้ checkpoint ที่ดีที่สุดแทน
`load_best_model_at_end=True` เก็บ `checkpoint-200` ไว้ให้อัตโนมัติอยู่แล้ว ไม่ต้องกู้เอง

**บทเรียน: 15 epoch มากเกินไปสำหรับคลัง 1.79M token** — จุดคุ้มค่าอยู่ที่ประมาณ **1 epoch**
รอบหน้าถ้าคลังยังขนาดนี้ ควรตั้ง `epochs: 2` แล้วพึ่ง `load_best_model_at_end` เป็นตัวตัดสิน
หรือเพิ่มคลังให้ใหญ่ขึ้นก่อน (เขียนบทหนังสือที่เหลือ 34 บท คือทางที่ตรงประเด็นที่สุด)

### บั๊กที่พบและแก้เพิ่มระหว่างรอบนี้

| # | อาการ | สาเหตุ | การแก้ |
|---|---|---|---|
| 1 | model card ที่ push ขึ้น HF แสดง `lora_r=?`, `lr=?`, `scheduler=?` และ effective batch ผิด (16 แทน 8) | `build_card()` อ่าน hyperparameter จาก `hp.json` ซึ่งเขียนตอนเทรน**จบครบทุก epoch**เท่านั้น — รอบนี้หยุดกลางคันเพื่อเลี่ยง overfit ไฟล์เลยไม่มี แล้ว fallback เป็น `"?"` กับ `grad_accum=16` | ถอยไปอ่านจาก `cfg.hp` (ค่าที่ใช้เทรนจริงเสมอ) เมื่อไม่มี `hp.json` |
| 2 | model card มีข้อความค้างจาก smoke test เดิมที่ไม่จริงแล้ว: "validation set มีเพียง 1 บล็อก" และ "PPL ที่ดีขึ้นเป็น noise" | ข้อความ hardcode ไว้ใน `CARD` template ตั้งแต่ตอนคลังมี val 1 บล็อกจริง | ลบข้อความที่ผูกกับตัวเลขเฉพาะรอบนั้นออก เหลือเฉพาะคำเตือนที่เป็นจริงเสมอ |
